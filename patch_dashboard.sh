sed -i '11,15s/useState/useState, useMemo/g' src/components/dashboard/DashboardModule.tsx
sed -i 's/const todaysSales = sales.filter(s => s.date === todayStrForProd);/const resetTimestamp = Number(localStorage.getItem("puremax_daily_reset") || "0");\n  const todaysSales = sales.filter(s => s.date === todayStrForProd \&\& new Date(s.createdAt || new Date()).getTime() > resetTimestamp);/g' src/components/dashboard/DashboardModule.tsx
sed -i 's/const todaysExpenses = expenses.filter(e => e.date === todayStrForProd);/const todaysExpenses = expenses.filter(e => e.date === todayStrForProd \&\& new Date(e.createdAt || new Date()).getTime() > resetTimestamp);/g' src/components/dashboard/DashboardModule.tsx
sed -i 's/const todaysProduction = production.filter(p => p.date === todayStrForProd);/const todaysProduction = production.filter(p => p.date === todayStrForProd \&\& new Date(p.createdAt || new Date()).getTime() > resetTimestamp);/g' src/components/dashboard/DashboardModule.tsx
